class Vino1{
    constructor(id, nombre, precio, iva){
        this.id = parseInt(prod.id),
        this.nombre = prod.nombre,
        this.precio = parseFloat(prod.precio),
        this.iva = parseFloat(prod.iva)
    }

    precioFinal(){
        let = resultado = parseFloat((this.precio * iva/100)+ this.precio)
        return resultado.toFied(2)
    }
} 